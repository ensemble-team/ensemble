<h1>New Track</h1>

<%= simple_form_for @track do |f| %>
  <%= f.input :title, placeholder: 'Track Title' %>
  <%= f.input :text, placeholder: 'Track Description' %>
  <%= f.file_field :avatar %>
  <%= f.button :submit, button_html: { value: 'Upload Track' } %>
<% end %>
